# JSON API
claroline_portfolio_bundle_api:
    resource: "@ClarolinePortfolioBundle/Controller/API/"
    prefix: apiv2
    type: api
    options:
        expose: true

claroline_portfolio_bundle:
    resource: "@ClarolinePortfolioBundle/Controller/"
    prefix: /
    type: annotation
    options:
        expose: true